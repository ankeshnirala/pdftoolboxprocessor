export enum FileStatus {
    Pending = "pending",
    Processing = "processing",
    Complete = "complete"
}

export enum QueueStatus {
    Processing = "processing",
    Complete = "complete",
    Waiting = "waiting",
    Failed = "failed"
}